Call Recorder update for Call-Recorder.apk

v1.02

in Settings in the app I must be able to choose source for mic audio input (if multiple input available) and speaker audio (if multiple available) and able to set volume level for speaker recording (and gain), and mic volume level and (mic gain) in the recording, both audio sources should be mixed into the same file.

another setting to be able to choose output type for the recording, wav (default), flac, and mp3, (also add more most common high quality fast efficiant codecs if there are ones available.

Add a setting to choose included contacts to be recorded (default all recordings are recorded), 
add exclusion contacts that are never recorded,
add file location of recordings (default is DCIM/recordings/call-recordings-datetime.file-ext 
add any other Settings, options that will be good professional options, 
allow auto start app (run in background) by default and allow disable auto start up toggle in settings!

Always use same key-store for each app made via github workflows so it can update correctly without requiring uninstallation

Name the final (debug enabled) full working released app (apk) Call-Recorder.apk

Save changes to changes.txt (create if not exists)

Tell me when ready to test (stay quiet after Tcknowledging you got the message / request / mission every time and stay quiet till its ready to test and respond only if fully complete  or if you need input from me or if I ask for an update)!

When giving final github release link (where applicable), make sure it points to the newest release but without the tag or filename so I can see the correct location without direct downloading the file as thats best practice!

Each update should use same key store so the app can update and not require uninstall of the app to update it.

github api tokens / passwords etc can be found in /home/jay/Documents/Scripts/AI/openclaw/password-vault/

Build the releases via github actions / workflows (not locally)


